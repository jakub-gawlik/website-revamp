---
import type { CollectionEntry } from "astro:content";
import { Image } from "astro:assets";

interface Props {
    post: CollectionEntry<"posts">;
}

const { post } = Astro.props;
const { title, description, date, tags, cover } = post.data;
---

<article
    class="group flex flex-col overflow-hidden rounded-lg border border-pastel-black/10 bg-pastel-white transition-shadow hover:shadow-md dark:border-pastel-white/10 dark:bg-pastel-black/30"
>
    {
        cover && (
            <div class="aspect-video overflow-hidden bg-pastel-black/5 dark:bg-pastel-white/5">
                <Image
                    src={cover}
                    alt={title}
                    width={800}
                    height={450}
                    class="h-full w-full object-cover transition-transform duration-300 group-hover:scale-105"
                />
            </div>
        )
    }
    <div class="flex flex-1 flex-col p-5">
        <div class="mb-2 text-xs font-medium text-foreground/60">
            <time datetime={date.toISOString()}>
                {
                    date.toLocaleDateString("en-US", {
                        year: "numeric",
                        month: "long",
                        day: "numeric",
                    })
                }
            </time>
        </div>
        <h3 class="mb-2 text-lg font-semibold">
            <a href={`/posts/${post.id}`} class="hover:underline">
                {title}
            </a>
        </h3>
        <p class="mb-4 flex-1 text-sm leading-relaxed text-foreground/70">
            {description}
        </p>
        {
            tags.length > 0 && (
                <ul class="flex flex-wrap gap-2">
                    {tags.map((tag: string) => (
                        <li class="rounded-full bg-pastel-green/40 px-2.5 py-1 text-xs font-medium text-foreground dark:bg-pastel-green/30">
                            {tag}
                        </li>
                    ))}
                </ul>
            )
        }
    </div>
</article>
